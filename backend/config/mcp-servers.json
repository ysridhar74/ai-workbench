[
  {
    "name": "filesystem",
    "description": "Read and write files on the local file system",
    "enabled": true,
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "${MCP_FILESYSTEM_ROOT}"],
    "tools": []
  },

  {
    "_comment": "Example: remote MCP server using modern Streamable HTTP transport (MCP spec 2025-03-26)",
    "name": "remote-example",
    "description": "Example remote MCP server over HTTP",
    "enabled": false,
    "transport": "streamable-http",
    "url": "http://localhost:8080/mcp",
    "headers": {
      "Authorization": "Bearer ${REMOTE_MCP_TOKEN}"
    },
    "timeout": 30000,
    "tools": []
  },

  {
    "_comment": "Example: legacy SSE-based MCP server (older MCP spec)",
    "name": "legacy-sse-example",
    "description": "Example legacy SSE MCP server",
    "enabled": false,
    "transport": "sse",
    "url": "http://localhost:9090/sse",
    "headers": {
      "Authorization": "Bearer ${LEGACY_MCP_TOKEN}"
    },
    "timeout": 30000,
    "tools": []
  }
]
