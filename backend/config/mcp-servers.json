[
  {
    "_comment": "mongodb-query is now handled natively in-process by MongoQueryService. Kept here for use with external tools like Claude Desktop.",
    "name": "mongodb-query",
    "description": "Natural language MongoDB querying — schema discovery, NL-to-aggregation-pipeline, and direct pipeline execution",
    "enabled": false,
    "transport": "stdio",
    "command": "npx",
    "args": ["tsx", "${MONGODB_QUERY_MCP_PATH}/src/index.ts"],
    "env": {
      "MONGODB_URI": "${MONGODB_URI}",
      "MONGODB_DB": "${MONGODB_QUERY_DB}",
      "OPENAI_API_KEY": "${OPENAI_API_KEY}",
      "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}",
      "LLM_MODEL": "${LLM_MODEL}",
      "LLM_BASE_URL": "${LLM_BASE_URL}",
      "QUERY_TIMEOUT_MS": "30000",
      "SAMPLE_SIZE": "200"
    },
    "tools": []
  },

  {
    "name": "filesystem",
    "description": "Read and write files on the local file system",
    "enabled": true,
    "transport": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-filesystem", "${MCP_FILESYSTEM_ROOT}"],
    "tools": []
  },

  {
    "_comment": "Example: remote MCP server using modern Streamable HTTP transport (MCP spec 2025-03-26)",
    "name": "remote-example",
    "description": "Example remote MCP server over HTTP",
    "enabled": false,
    "transport": "streamable-http",
    "url": "http://localhost:8080/mcp",
    "headers": {
      "Authorization": "Bearer ${REMOTE_MCP_TOKEN}"
    },
    "timeout": 30000,
    "tools": []
  },

  {
    "_comment": "Example: legacy SSE-based MCP server (older MCP spec)",
    "name": "legacy-sse-example",
    "description": "Example legacy SSE MCP server",
    "enabled": false,
    "transport": "sse",
    "url": "http://localhost:9090/sse",
    "headers": {
      "Authorization": "Bearer ${LEGACY_MCP_TOKEN}"
    },
    "timeout": 30000,
    "tools": []
  }
]
