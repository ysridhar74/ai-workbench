# Enforce exact version pinning — required for company artifact repository
# which has a 2-day propagation delay. Caret/tilde ranges can resolve to
# packages not yet available in the mirror, causing install failures.
save-exact=true
